package com.sunyaxin.common.config;

import org.springframework.context.annotation.Configuration;

/**
 * Redis配置类
 * 注意：实际的Redis配置已移至 RedisCacheConfig
 */
@Configuration
public class RedisConfig {
    // RedisTemplate和CacheManager配置已移至 RedisCacheConfig
    // 此类保留用于兼容性
}
